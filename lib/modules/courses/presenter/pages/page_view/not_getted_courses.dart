import 'package:flutter/material.dart';

class NotGettedCourses extends StatelessWidget {
  const NotGettedCourses({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
